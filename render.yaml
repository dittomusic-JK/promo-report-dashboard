services:
  - type: web
    name: promo-report-dashboard
    runtime: node
    buildCommand: npm install && npx playwright install chromium --with-deps
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
